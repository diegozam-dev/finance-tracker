import LoginContainer from './components/AuthContainer';
import RegisterCard from './components/RegisterCard';

const Register = () => {
  return (
    <LoginContainer>
      <RegisterCard />
    </LoginContainer>
  );
};

export default Register;
